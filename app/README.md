# App Layer

This directory contains the Streamlit UI for the Text2SQL Assistant.

Run it with:

```bash
streamlit run app/streamlit_app.py
```

If `streamlit` is not installed yet, create the local repo venv first:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
```
